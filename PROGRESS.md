# 進捗メモ

最終更新: 2026-08-24

## 弟テストのフィードバック

- GitHub Pages（`https://shota-tdkr.github.io/weather-vapor-app/`）を使って非同期テストを開始
- 「実験モードって何？」→ 機能ではなく**言葉が伝わらない**という反応。ボタン・状態表示の文言を「実験モード／通常モード」という抽象的な呼び方から、「高さだけ動かせるようにする／地図で動かせるようにする」という、押した後に何ができるかを直接言う表現に変更した
- 内部のロジック（`mode`変数など）や設計自体（通常＝地図上の位置操作、高さだけ＝専用レバー操作）は変えていない。表示文言だけの修正
- 「水滴が消えない、常についている」→ バグではなく調整不足。山の影響半径（`MOUNTAIN_INFLUENCE_RADIUS`）が180で、400×320の舞台の71%が常に山の影響下に入ってしまい、どこにドラッグしても高さが0に戻らなかった。半径を120（影響範囲34.9%）に縮小し、画面の端まで動かせば明確に「山の影響なし」に戻れるようにした
- 上記の修正後も直らない、という報告 → 実際にはブラウザ側のstyle.cssキャッシュ（`cache-control: max-age=600`）が原因で、GitHub Pagesのサーバー自体は最新版を返していた。強制リロードで解決。再発防止として`index.html`の`style.css`/`script.js`読み込みに`?v=1`というバージョンクエリを付けた。**今後style.cssかscript.jsを編集したら、この番号を両方とも1つ上げること**（忘れると同じキャッシュ問題が再発する）

## 画面構成の方針転換（2026-08-24）

①②（計算・雲のできる仕組み）と③④（前線・気圧配置）は同時に見せる必要がない、という判断で、日本地図（本州・北海道・四国・九州のフルマップ）を画面から外した。まだ気圧配置・前線・地点表示という「地図が意味を持つ機能」が何もない段階で地図を映し続けるのは、ただの視覚的なノイズになっていたため。

- 地図の座標データはコミット `d805177` までに残っている（`git show d805177:index.html` などで取り出せる）
- ③④（前線・低気圧・高気圧・地点表示）に着手するタイミングで、地図をセットで復活させる予定。今すぐモード切り替えで両方持たせることも検討したが、今は地図側に機能差がなく「見た目が変わるだけ」になるため見送った

## 実装済み

- **山ひとつ＋空気の塊のシンプルな舞台**: 日本地図の代わりに、地面＋山（三角形ひとつ）＋空気の塊（○）だけのシーン。山の頂点（yが最小の点）を「山の位置」として使い、○が近づくほど高さが自動で上がる
- **空気の塊**: pointerdown/pointermove/pointerupでドラッグ可能な円オブジェクト（mouse/touch共通）
- **温度計・水蒸気ゲージ**: 高さから気温・飽和水蒸気量（教科書の対応表を線形補間）をリアルタイム表示。初期値は5℃・6.8g/m³（＝ほぼ飽和状態からスタート）
- **モード切り替え**: ボタンで切り替え。「○は地図で動かせます」状態は○を自由にドラッグでき、山への距離で高さを自動計算する。「○は高さだけ動かせます」状態では○は完全に固定され、代わりに専用の「高さレバー」を縦にドラッグして高さだけを直接操作する。位置操作と高さ操作を別々のUI要素に分離することで、「山から離れていく」ような視覚的な誤解を避けている
- **高さの表示**: 実単位ではなく0〜100の相対値で表示。実際の値（メートルなど）を主張しないことで、物理的な精度の議論を避ける設計
- **コップの水滴（凝結エフェクトの第一弾）**: コップの中には常に中身（冷たい飲み物）を描画。保有水蒸気量が飽和水蒸気量を超えた分だけ、水滴が液体に近い下側から上へ1つずつ増えていく。「冷えたコップに水滴がつく」という生徒が実生活で見たことのある現象に、抱えきれない水蒸気＝結露という因果を直結させる狙い（弟テストで「雨粒が現れてる！」と好反応。①②の核が体感として伝わった手応え）
- **上昇エフェクト**: 高さが上がった瞬間だけ、空気の塊の下から小さな△矢印が3つフワッと浮かんで消える演出。山と同じ形（△）を使って視覚言語を統一。空気の塊・矢印とも「薄い塗り＋濃紺の輪郭線」のアウトラインスタイルにすることで、背景が山（濃紺）でも空（薄いグレー）でもどちらでも視認できるようにしている
- **凡例（デバッグ用）**: ○△の意味とモードの違いをページ下部に簡易表示。CLAUDE.mdの正式なチュートリアル（初回起動時の吹き出しガイド）とは別物で、開発・非同期テスト中の仮の説明

対応コミット: `35b0efc`〜`d805177`（日本地図があった版）。山ひとつのシンプル化＋コップの水滴は次コミットで対応

## 既知の保留事項

- **気圧**: 未モデル化。CLAUDE.mdの方針（物理的厳密さより教育的な分かりやすさ優先）に沿って、「高さ→気温」を単純な係数（LAPSE_RATE）で直接結んでいる
- **雲の発生演出**: コップの水滴は実装したが、空気の塊自体から雲が湧く演出（CLAUDE.md本来の凝結エフェクト）はまだ未実装

## 未着手（CLAUDE.mdの画面設計より）

- 日本地図・季節スライダー・状態パネルの復活（③④に着手するタイミングでセットに）
- 前線・低気圧・高気圧の配置、地点表示（札幌・新潟・東京・大阪・那覇）
  - 沖縄（那覇）は地図のviewBoxから大きく外れる位置にあるため、実際の日本地図でよくある「別枠インセット表示」での対応を、地点表示の実装時にあわせて行う
- 変化ログ（因果を段階表示するテキスト、メッセージ定義オブジェクトへの集約）
- 露点モード
- お題モード
- チュートリアル

## スケジュールとの対応

- エントリーは完了済み
- 〜8/31: コア実装、弟への非同期テスト開始 ← 今ここ
- 9/1〜9/9: お題モード、変化ログの作り込み、週2回程度の弟テスト
- 9/10: 機能凍結
- 9/11〜9/12: デモ動画、作品概要、README
- 9/13 午前中: 提出
